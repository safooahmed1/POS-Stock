{
  "name": "inventory-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --names server,client --prefix-colors blue,green \"node --watch --env-file=.env api/index.js\" \"cd client && npm run dev\"",
    "dev:server": "node --watch --env-file=.env api/index.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "postbuild": "cp -r client/dist dist",
    "start": "node --env-file=.env api/index.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "express": "^4.21.1",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
